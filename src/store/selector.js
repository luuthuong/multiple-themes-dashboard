const getItem=(item)=>item.toggleTheme;

const getColor=(item)=>item.color;

const getTheme=(item)=>item.theme;

const getDataTable=(item)=>item.dataTable

const selector={
    getItem,
    getColor,
    getTheme,
    getDataTable
}

export default selector;